import { DestGraphDirective } from './dest-graph.directive';

describe('DestGraphDirective', () => {
  it('should create an instance', () => {
    const directive = new DestGraphDirective();
    expect(directive).toBeTruthy();
  });
});
